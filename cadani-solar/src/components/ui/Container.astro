---
interface Props {
  as?: 'div' | 'section' | 'article';
  className?: string;
}

const props = Astro.props as Props;
const { as: Tag = 'div', className = '' } = props;
---

<Tag class:list={['container', className]}>
  <slot />
</Tag>
